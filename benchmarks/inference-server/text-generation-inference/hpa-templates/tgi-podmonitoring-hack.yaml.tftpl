apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: "tgi-podmonitoring-hack"
  namespace: ${namespace}
spec:
  selector:
    matchLabels:
      app: tgi
  endpoints:
  - port: 8080
    interval: 20s
