apiVersion: v1
kind: Service
metadata:
  name: tgi-hack
  namespace: ${namespace}
  labels:
    app: tgi
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: tgi
